# TI Drive Companion

An interactive visual comparison tool for Terra Invicta ship drives.

## Run locally

Requires Node.js 22.13 or newer.

```bash
npm install
npm run dev
```

The app loads `public/data/TIDriveTemplate.json` at runtime. Replace that file
with an updated export to refresh the drive catalog without changing the UI.
